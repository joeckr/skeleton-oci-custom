apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.service.name }}
  labels:
    app: {{ .Values.template.name }}
spec:
  type: NodePort
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.template.port }}
      protocol: TCP
      name: {{ .Values.service.name }}
  selector:
    app: {{ .Values.template.name }}
